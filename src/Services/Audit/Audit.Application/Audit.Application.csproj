<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Audit.Domain\Audit.Domain.csproj" />
    <ProjectReference Include="..\Audit.Contracts\Audit.Contracts.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\Ehs.SharedKernel\Ehs.SharedKernel.csproj" />
    <!-- IIntegrationEventHandler<T> — this project hosts the Finding/Action Plan event consumers. -->
    <ProjectReference Include="..\..\..\BuildingBlocks\Ehs.EventBus\Ehs.EventBus.csproj" />
    <!-- Consuming Finding/Action Plan Service's published event *contracts*
         (DTOs only, no DB access) — never a reference to their Infrastructure/DB. -->
    <ProjectReference Include="..\..\Finding\Finding.Contracts\Finding.Contracts.csproj" />
    <ProjectReference Include="..\..\ActionPlan\ActionPlan.Contracts\ActionPlan.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- CLAUDE.md §3/§9: FluentValidation for centralized Application-layer validation. -->
    <PackageReference Include="FluentValidation" Version="12.0.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
